# G8a 提审前审计 — 2026-09-19(picbox / tunekit / photolift,三个只读 agent)

> 静态审计,未构建未运行。修完 P0 + P1 → analyze/test → 重新出包 → 才提交。

## photolift(PixelLift)— FIX P0 FIRST
iOS 上 AI 引擎**真的存在**(ncnn CPU,Real-ESRGAN general-x4v3,非 Dart 兜底)。提交前须真机确认结果页显示「AI · CPU」而不是「Basic resample」。
- **P0-1 锁屏即冻结**:无 `isIdleTimerDisabled` / `beginBackgroundTask` / 生命周期处理。iPhone 只有 2 个大核,1200×1600 约 2 分钟、12MP@2x ≥6 分钟;默认 30s 自动锁屏 → 任务挂起,被系统回收后回来是首页、无结果无提示。修:`UpscaleBridge.swift` startJob 置 `isIdleTimerDisabled = true`,完成块复位;包一层 `beginBackgroundTask`;进度环下加一句「处理期间请保持 PixelLift 打开」。Android 用 `FLAG_KEEP_SCREEN_ON`。
- **P0-2 关键词含竞品名(2.3.7)**:`store/aso.md:43,49` 的 `remini,pixelup` / `Remini,你我当年,waifu2x`。
- P1-1 双击「开始」UI 失步(`lift_screen.dart:94` 加 `if (_phase == running) return;`)。 P1-2 开始后 1 秒内点取消会丢失(`PhotoLiftEngine.mm:65` 重置了 cancelFlag;在 processRGBA 前再查一次,进度回调里查到就 `engine.cancel()`)。 P1-3 「几分钟内」的速度承诺在 iPhone 上不成立,改文案 + 用真机实测值播种 ETA。 P1-4 ≥6MP 的照片 4x 与 2x 输出同为 24MP,Pro 卖的「4x 海报级」落空 → 配置页给提示。 P1-5 副标题 "Restore Old Photos" 偏强(只做放大+降噪),考虑 "Sharpen"。 P1-6 标题在 listing.md 与 aso.md 不一致。
- P2:编码阶段取消被忽略;存相册失败丢原生错误信息;JSON 写失败后孤儿清理会删掉成品;缺 MIT 声明(Real-ESRGAN-ncnn-vulkan 是 MIT,`THIRD_PARTY_NOTICES.md` 误标 BSD-3);买 Pro 后本页锁标不刷新;透明 PNG 变黑底;无 `PrivacyInfo.xcprivacy`。

## picbox(PicWorks)— FIX P0 FIRST
- **P0-1 买了 Pro,当前工具页不解锁**:工具页不监听 `PicboxStore`(`tool_flow.dart:78`)。在 Convert 里点锁着的 WebP → 付费 → 弹「已解锁」→ 再点 WebP **又弹付费页**。这是审核员最自然的路径,读起来就是「购买不生效」(2.1 / 3.1.1)。修:`_ToolScaffoldState.initState` 里 `store.addListener(() { if (mounted) setState((){}); })`,dispose 移除。
- P1-1 iOS 可见文案提 Android(`picker.dart:112` "HEIC needs Android 9+")→ 2.3.10。 P1-2 「无损、一个像素都不重编码」在 iPhone 上是假的:image_picker_ios 会把每张图解码后以 quality 1.0 重编码,且「处理前」大小被夸大 → 改文案(listing.md:25,61 + `metadata_screen.dart:87`)。 P1-3 HEIC 在 iOS 被标成 JPEG(文件名恒为 .jpg),Convert 显示「JPEG → JPEG / 同格式重编码」,而副标题卖的正是 HEIC to JPG。 P1-4 「最长边」输入框没法打字(onChanged 即时钳到 min 16 并回写文本)。
- P2:「已保存」后按钮仍可点 → 重复存相册;文案说质量下限 20、代码是 15;「清理临时文件」清不到 image_picker 在 tmp 根目录的大文件;工作目录创建失败静默;48MP 照片被裁剪/水印拒绝(有明确提示);首页磁贴双击推两层;listing.md 与 aso.md 的标题/副标题/关键词不一致,缺 What's New 与 30 字副标题,`store/appstore.md` 不存在。

## tunekit(TuneBench)— 无 P0
- P1-1 付费页卖「无限练习历史」,实际最长只能看 30 天(`pro.dart:71`、`log_page.dart:329`、listing)。 P1-2 **没有给审核的后台音频说明**(AutoSnore 就是因 2.5.4 被拒的)→ ASC Notes 写:Beat 页 → Play → 锁屏,节拍器继续响;麦克风从不在后台使用。 P1-3 `installTap` 用了可能过期的 format,会抛 Swift 抓不住的 NSException(节拍器在响时切到调音页、尤其 AirPods)→ `format: nil`。 P1-4 麦克风可能在后台被打开(start 在途时切后台)。 P1-5 设置页/隐私页盖住调音页时麦克风仍开着,与隐私文案矛盾。 P1-6 乐器预设下状态行与表盘矛盾(一个按最近半音、一个按目标弦)。 P1-7 键盘图只有 2 个八度,13 和弦等高位音丢失 → `octaves: 3`。 P1-8 关键词含竞品名(`tonalenergy,guitartuna`)。
- Android 专属 P1-A1(Play 提交前修):去系统设置授权麦克风回来后不会自动重启,CheckPage 是死路。
- 需真机验:节拍器响着时开调音页再锁屏;`.measurement` 模式输入增益低,`quietRms 0.012` 可能让正常弹奏显示「太安静」。
