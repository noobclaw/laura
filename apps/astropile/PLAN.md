# AstroPile（星野叠加）— 里程碑 M1

> 取题：BACKLOG 总览 #6「天文叠加 AstroPile(多帧配准 + 堆栈)」指数 **58**、`⚪排队`、队列**唯一待做项**（2026-09-08 由观察池转入，双硬门 缺口 11/15 ✅ + 付费 16/25 ✅ 两项全过）。
> 本轮工厂交付 **M1**（BACKLOG 已拆 M1/M2/M3，符合 08-30 放宽条款③「大型项按里程碑占用多轮工厂」）。

## 一句话定位

把手机连拍的十几张夜空照片对齐叠加成一张更干净的照片；**每一帧都告诉你对上了没有、对不上是为什么**，可以逐帧勾除后重叠。

## 差异化楔子（G2 硬性一句话）

> **用户为什么选我而不是 `AstroShader` / `Star Stacker` / 免费的三支 Stacker：**
> 免费同型三支 `SharpStacker` ★1.00/1 条、`Astro Stacker` ★1.00/4 条、`Eagle Image Stacker` ★1.00/1 条 —— **免费侧不是不做，是做砸了**：叠出来一团糊，而且不告诉你为什么。
> **AstroPile 把「对齐」这一步变成可见、可诊断、可干预的**：逐帧给出检出星点数 / 匹配星点数 / 残差(px) / 质量分，失败帧写明原因（星点太少 / 找不到匹配星阵 / 残差过大 / 画幅不一致），并允许**勾除后立即重叠**。这是 BACKLOG 楔子表里唯一被标为「**强**」并指定为**主打句**的一条。

楔子副线（按 BACKLOG 强度表，**不得作主打句**）：
- 一次买断、不按张收费（中等强度，跨品类外推）。
- 照片不出手机（**零强度**，只作副线；不写进商店标题/首屏主句）。

## G2 招牌功能架构评审（可靠性机制选型，写码前定死）

| 机制 | 选型（定死） | 为什么不是另一种 |
|---|---|---|
| 星点检出 | **游程(run)级连通域 + 并查集**，全分辨率直接做 | 像素级 label 数组在 12MP 上要 `Int32List(12M)=48MB`；游程只在阈值以上的像素上分配，内存与星点数成正比。**不下采样**，因为下采样会把质心精度从亚像素降到 ±1.4px，而对齐残差门槛就是 2px |
| 配准 | **星阵(三角形)不变量匹配 + RANSAC + 相似变换闭式最小二乘** | 角点/特征点法在星图上不成立（星点彼此无结构、互相不可分辨）——这正是参考实现 `astroalign` 的立论。相似变换(4 dof)覆盖手持/赤道仪的平移+旋转+微缩放；不做仿射，自由度越高越容易在少量星点上过拟合 |
| 内存 | **单帧进出 + 对齐后落盘 raw + 叠加按横条(band)流式** | BACKLOG 记「32 帧 × 12MP × float32 ≈ 4.6GB」。实际做法：任一时刻内存里只有 1 帧（解码 RGBA 48MB + RGB 副本 36MB + 亮度 12MB ≈ 96MB 峰值），中值叠加所需的「同一像素的 N 个值」由**按条读盘**提供，峰值 = 条高 × 宽 × 3 × N ≈ 24MB |
| 耗时 | **每帧一个 `Isolate.run`，帧与条的边界即取消点** | 分钟级任务不能占住 UI 线程。取消粒度 = 一帧（数秒），足够；换长驻 isolate + 端口信令会引入泄漏与状态同步风险，收益只是把取消延迟从数秒降到亚秒 |
| 原生依赖 | **M1 不引入 FFI，纯 Dart AOT + `TypedData`**；见下「对 BACKLOG 风险 3 的答复」 | 见下 |
| 结果落地 | 合成结果先落 **raw** 再拉伸编码；拉伸参数改一次不重算叠加 | 让用户拖黑点/亮度滑杆时是**即时预览**（在 1080px 缩略图上算），导出才走全分辨率 |

### 对 BACKLOG「三条工程风险」的逐条答复（G2 必答）

1. **内存 4.6GB** → 见上表。三段式：`对齐(逐帧) → 落盘 → 按条叠加`。峰值 ≈ 96MB，与帧数无关。
   代价是**磁盘**：对齐帧以 8-bit RGB raw 落盘，`W×H×3 × N`。12MP × 32 帧 ≈ 1.17GB。处置：
   - 免费层封 **6 帧**（≈220MB），Pro 封 **32 帧**；
   - 提供**工作分辨率**开关（全分辨率 / 半分辨率），半分辨率磁盘与耗时都降到 1/4；
   - 开跑前在界面上**明写本次预计占用多少临时空间**；写盘失败（`FileSystemException`）单独归一成「存储空间不足，请清理后重试或改用半分辨率」，**不静默**；
   - 结束或取消后立即删除整个临时目录，冷启动再兜底清一次（同 picbox `WorkDirs.clearAll`）。
2. **耗时分钟级** → 每帧一个 isolate，**进度按帧+按条上报**，随时可停止；停止后已完成的逐帧报告仍然保留可看。**不做后台通知/前台服务**：任务生命周期与前台界面绑定，离开页面即停止并说明——比假装能在后台跑完更诚实（这也是免费三支 ★1.00 的高发死法：跑一半黑屏）。
3. **原生依赖（FFI）** → **M1 明确不做，理由写死在这里**：
   - 像素级热点只有两处：`warp`（每输出像素 4 次乘加的双线性）与 `stack`（每像素 N 次读+一次中值）。两者都是 `Uint8List/Float32List` 上的顺序循环，Dart AOT 与 C 的差距在 2–3 倍量级，不是数量级；
   - 真正的大头是 **JPEG 解码**（`package:image` 纯 Dart），而它不是我们写的循环，加 FFI 也绕不开，除非换原生解码器——那是 M2 的事（`flutter_image_compress` 只能整图重编码，拿不到像素）；
   - 引入 FFI 意味着 Android CMake + iOS SPM/podspec 两套构建，**本机不能跑 `flutter build`（8GB 内存，PIPELINE 明令）**，等于把不可本地验证的构建风险塞进 M1；
   - **可证伪**：M1 出包后真机实测 12 帧 × 12MP 全分辨率若超过 **5 分钟**，M2 第一优先级即改为 FFI（或原生解码）。这条写进 M2 待办。

## G2 权限流程评审

| 权限 | 何时要 | 拒绝时 | 永久拒绝 |
|---|---|---|---|
| 相册**读** | 不需要。导入走系统 Photo Picker / PHPicker，两端都不需要运行时权限 | — | — |
| 相册**写**（保存结果） | 点「保存到相册」时由 `gal` 请求 | 返回可见原因，并提示可改用「分享」 | 提供「去系统设置」按钮（`permission_handler.openAppSettings`） |
| 相机 | **M1 不用**（连拍内置是 M2）。Android 侧 `CAMERA` 从合并清单里 `tools:node="remove"`；iOS 侧不写 `NSCameraUsageDescription` | — | — |
| 网络 | **不申请**。`INTERNET` 从合并清单里移除 | — | — |

iOS 侧平行项（PIPELINE「iOS 平行检查」逐条）：
- `NSPhotoLibraryUsageDescription`（选片）、`NSPhotoLibraryAddUsageDescription`（保存）两条写进 `Info.plist` 并**说明用途**；**不写** `NSCameraUsageDescription`（M1 不碰相机，多写会被 ITMS-90683 拒）；
- 显示名 `CFBundleDisplayName=AstroPile` + `zh-Hans.lproj/InfoPlist.strings` 给中文名，与 Android `values/`+`values-zh/` 成对；
- **不提交 `ios/Podfile`**（SPM 铁律）；`permission_handler` 的宏由本 app 自己的 plist usage key 开启；
- `TARGETED_DEVICE_FAMILY = 1`（只 iPhone）；两端都锁竖屏；
- `share_plus` 传 `sharePositionOrigin`；
- 无平台 channel、无小组件、无后台模式 → 无 iOS-only 缺口。

## 功能清单（v1 = M1，只做核心）

**做**
1. **导入**：系统相册多选（JPEG/PNG/HEIC，HEIC 走系统解码转 JPEG 中间件）。列出每帧：文件名、尺寸、ISO / 快门 / 焦距（EXIF）。
2. **一致性预检**：画幅不一致的帧直接标红不可用；**曝光参数与多数帧不一致的帧给黄色提醒**（可保留可勾除）——BACKLOG M1 明列「标出曝光参数不一致的帧」。
3. **参考帧**：默认第 1 帧，可改选任意一帧。
4. **对齐**：星点检出 → 星阵不变量匹配 → RANSAC → 相似变换 → 双线性反向重采样。
5. **逐帧质量报告（楔子，免费层可用）**：检出星点数 / 匹配星点数 / 残差 RMS(px) / 质量分 0–100 / 失败原因；**逐帧可勾除后重叠**。
6. **叠加**：均值（免费）/ **中值**（Pro，去飞机、卫星、热噪）。按条流式，越界像素不参与平均（不会把边缘压暗）。
7. **拉伸**：黑点 / 亮度(midtone) / 饱和度 三个滑杆，1080px 实时预览；默认给一组自动值（黑点取 0.5 百分位、midtone 把中位数拉到 0.25）。
8. **导出**：保存到相册 / 系统分享，JPEG q95 全分辨率。
9. **工作分辨率**：全 / 半。

**不做（明确留给 M2/M3，商店文案里一个字都不许提）**
- kappa-sigma 剪切、最大值叠加（星轨）、暗场/平场标定、app 内连拍 → **M2**
- RAW(DNG)、批次管理、失败帧可视化诊断报告 → **M3**

## 页面结构

```
首页 Home
 ├─ Hero（夜空渐变 + 已叠加张数 + 主行动「选择照片开始叠加」）
 ├─ 三步说明条（选片 → 对齐 → 叠加）
 └─ 离线声明
帧列表 FramesScreen
 ├─ 顶部汇总（N 帧 · 画幅 · 预计临时占用）
 ├─ 逐帧卡片（缩略图 / 名称 / ISO·快门 / 参考帧徽标 / 勾选 / 警告）
 ├─ 设置区（叠加方式 平均|中值(Pro) · 工作分辨率 全|半）
 └─ 主按钮「开始叠加」
进度 RunScreen
 ├─ 环形进度 + 阶段文案（对齐第 i/N 帧 / 合成第 j/M 条 / 导出）
 ├─ 逐帧结果流（成功打勾 + 残差；失败标红 + 原因）
 └─ 「停止」
结果 ResultScreen
 ├─ 预览（可点开全屏）
 ├─ 拉伸滑杆 ×3
 ├─ 叠加报告卡（参与 N 帧 / 排除 M 帧及原因 → 可展开逐帧表）
 └─ 保存到相册 · 分享 · 重新叠加
```

## 参考实现分析（PIPELINE / CLAUDE.md 选题硬性标准第 ①②条）

### `quatrope/astroalign` — **MIT（permissive）**
许可证允许直接依赖或内嵌 + 署名。本项目**未复制任何一行 Python 代码**（Python→Dart 无法复制），实现依据是下面这份自写规格；**署名写进 app 内「关于 → 开源致谢」与 `store/listing.md`**。

规格（读 `_ref/astroalign/astroalign.py` v2.6.2 后自写，含我方改动与理由）：

1. **控制点**：星点按 flux 降序取前 `max_control_points = 50`。少于 3 个 → 判失败（原实现抛 `ValueError`，我方转成用户可读的「星点太少」）。
2. **不变量**：对每个星点取 `NUM_NEAREST_NEIGHBORS = 5` 个最近邻（含自身），在这 5 点里枚举 `C(5,3)=10` 个三角形；三角形不变量 = 边长排序后 `[L3/L2, L2/L1]`（对平移/旋转/缩放不变）。三角形顶点按 `(a,b,c)` 规范排序：`a = L1∩L2`、`b = L2∩L3`、`c = L3∩L1`（`L1<L2<L3`），**保证两图同一星阵的顶点一一对应**。
3. **去重**：不变量重复的三角形只留一个。
4. **匹配**：源不变量与目标不变量在二维不变量空间里做半径搜索，`r = 0.1`（原实现经验值，我方沿用）。每一对匹配产出 3 组点对。
5. **RANSAC**：`PIXEL_TOL = 2`（像素残差阈值）、`MIN_MATCHES_FRACTION = 0.8`、`min_matches = max(1, min(10, floor(0.8 × 匹配数)))`。随机遍历单个三角形匹配作最小样本；用它拟合的变换去测全体，内点数达标即接受，随后**用全体内点重拟合 3 轮**。
6. **变换**：原实现调 `skimage` 的 `similarity`。我方自写闭式最小二乘（见下），结果等价。
7. **重采样**：原实现 `skimage.warp(order=3)` 双三次 + `cval=median`。我方用**双线性**（移动端算力权衡，星点是弥散斑，双线性的差别在肉眼与残差上都不显著）+ 越界像素**不参与叠加**（原实现填中位数，那会在边缘留一圈假背景；我方按覆盖计数求平均，更干净）。
8. **原实现依赖 `sep`（Source Extractor）做星点检出**，`sep` 是 **LGPL**，且是 C 扩展，移动端不可用 → **星点检出这一段由我方从零设计**（见下「星点检出」），只保留其接口语义：`detection_sigma=5`、`min_area=5`、按 flux 降序。

**相似变换闭式最小二乘（我方推导，非抄）**
模型 `u = c·x − d·y + tx`，`v = d·x + c·y + ty`（`c = s·cosθ`，`d = s·sinθ`）。以质心平移后：
```
S  = Σ(x'² + y'²)
c  = Σ(x'·u' + y'·v') / S
d  = Σ(x'·v' − y'·u') / S
tx = ū − (c·x̄ − d·ȳ)
ty = v̄ − (d·x̄ + c·ȳ)
```
逆变换（叠加与重采样都要）：
```
k = c² + d²
x = ( c·(u−tx) + d·(v−ty) ) / k
y = ( −d·(u−tx) + c·(v−ty) ) / k
```

**星点检出（我方规格，无参考实现）**
1. 亮度 `Y = (77R + 150G + 29B) >> 8`，`Uint8List`。
2. 背景：按 `W×H/40000` 步长网格采样约 4 万点，做 3 轮 3σ 裁剪，得 `bkg`、`sigma`（`sigma` 下限 1.0，防全黑图除零）。
3. 阈值 `T = bkg + 5σ`（上限 254）。
4. **游程连通域**：逐行收集 `Y > T` 的游程；与上一行游程按 8 邻接（列区间相交或相邻）并查集合并。每个游程携带 `area / flux / Σfx / Σfy`，`flux = Σ(Y − bkg)`。
5. 过滤：`area < 5` 丢弃（热噪/坏点）；`area > 4000` 丢弃（月亮、地景、云）。
6. 质心 `(Σfx/flux, Σfy/flux)`，按 `flux` 降序取前 50。
7. **保护**：游程总数超过 40 万 → 判「画面过亮或不是星野照片」并把阈值抬到 `bkg + 8σ` 重试一次，仍超则该帧判失败并写明原因（不静默、不 OOM）。

### `verlab/accelerated_features`（XFeat, **Apache-2.0**）
BACKLOG 把它列为可内嵌的特征匹配备选。**M1 不用**：它是需要权重文件的深度模型，包体与推理运行时的成本在「星点匹配」这个已经有闭式解的问题上不划算。留作 M2 里「星点太少的宽视场/带地景画面」的备选路线，届时按 Apache-2.0 内嵌 + 署名。

### `gmberton/vismatch`（BSD-3）
选型对照用，**未打包、未引用**。

### `deepskystacker/DSS`（NOASSERTION = 视同不可用）
**M1 未读、未参考、未使用**。M1 的叠加只有均值/中值两种教科书组合方式，不需要参考任何实现。M2 的「kappa-sigma 剪切 + 暗场/平场标定」才需要读 DSS —— 届时按 CLAUDE.md 固定两步走：**先读代码写成中文规格（写进本文件「参考实现分析」），再只看规格用 Dart 从零实现，不复制任何一行代码、不移植任何资源文件**。

> **许可证处置汇总**：M1 只参考 `astroalign`(MIT)，**未复制代码与资源**，署名保留。其余三个参考项目 M1 全部未使用。

## 定价

- **免费**：最多 **6 帧**/次、均值叠加、**逐帧质量报告完整可用**（楔子不设门）、全分辨率导出、无广告、无水印。
- **AstroPile Pro（一次性买断）**：**$3.99 / ¥28**（BACKLOG 定价：定在 `AstroShader` $1.99 与 `Star Stacker` $3.99 之间偏上；纪律 AG 明文「更便宜」撑不住品类）。
  - 最多 **32 帧**/次
  - **中值叠加**（去掉飞机、卫星、热噪）
  - 记住叠加设置
- 商品 ID：`com.noobclaw.astropile.pro_unlock`。每个免费门都先弹 `showProSheet`，`buyPro()` 只在 `pro.dart` 里出现。
- 价格一律走 `ProPriceText` / `PurchaseService.price` 取商店本地化价，`$3.99` 只作接口未答时的兜底。

## 商店定位

- 分类：Photo & Video（主）/ Utilities（副）
- 关键词：`astrophotography` / `image stacking` / `star trails` / `noise reduction` / `long exposure` / `night sky` / `milky way`；**避开 `stacker` 词根**（`Star Stacker` / `AstroShader` / `SharpStacker` / `Astro Stacker` 四支已占）
- 命名预检（2026-09-10 实做）：App Store `astropile` 美区 10 条结果**无同名**（最近的是 `AstroPill`，占星类）、中区 **0 条**；Google Play 搜 `astropile` / `astro pile` **无同名**。备选名 `NightStack` 同样无冲突，未启用。

## 真机验收清单（G7，用户照着点）

1. 冷启动 → 首页渲染完整、深浅色都正常。
2. 点「选择照片」→ 系统相册多选 **不出现权限弹窗**（Photo Picker）；选 8 张夜空连拍。
3. 帧列表：每帧显示 ISO/快门；故意混入一张**不同画幅**的照片 → 该帧标红且不可勾选；混入一张**曝光不同**的 → 黄色提醒但仍可选。
4. 开始叠加 → 进度环走动、逐帧结果一条条出现；**中途点「停止」→ 按钮立刻变「正在停止…」、进度环转黄**，当前这一张处理完就停（真机上单帧可能十几到几十秒，这是已知口径，不是卡死），已完成的帧报告仍在。
5. 重新开始 → 跑完 → 结果页出预览；拖动黑点/亮度/饱和度滑杆 **预览实时变化**。
6. 「保存到相册」→ 首次弹系统权限；**拒绝** → 出现具体原因 + 「去系统设置」；允许后再点 → 相册里能看到全分辨率成片。
7. 「分享」→ 系统分享面板出现（iPad 上不崩）。
8. 免费状态下选 **7 张** → 弹 Pro 解锁页（列出 Pro 加了什么 + 商店真实价格 + 恢复购买），**不直接跳付款**。
9. 选「中值叠加」→ 同样弹 Pro 解锁页。
10. 设置页切「English / 中文」→ 全界面（含帧列表、报告、错误提示）立即切换。
11. 只选 1 张 → 明确提示「至少需要 2 张」，不崩。
12. 选一批**没有星星**的照片（白墙/室内）→ 报「星点太少」并指名是哪几帧，不崩、不静默。

## 状态

- 2026-09-10：M1 代码完成，本地 `flutter analyze` / `flutter test` 通过，G8a 提审前审计已做（见下），CI 出包。**未真机验收**。

---

## G8a 提审前审计（2026-09-10，独立只读 agent）

按 PIPELINE「G8a 提审前审计」8 个面 + 自加一面「算法与并发正确性」跑了一轮。**结论:1 个 P0、10 个 P1,已全部修完;修完重跑 `flutter analyze` 零 issue、`flutter test` 38 绿。**

### P0(已修)
- **`FramesScreen` 一进就崩**:`late String _majorityExposure` 在 `_recompute()` 的统计循环里被 `_precheck()` 读到,而它要到循环**之后**才赋值 → `LateInitializationError`。选完照片就白屏,**App 的全部功能一个都到不了**。改成普通字段带默认值(空串 = 「还没有曝光结论」)。
  **⚖️ 这条最值得记住的地方**:`flutter analyze` 零 issue、31 个引擎测试全绿,**都测不到它** —— 因为测试全在 engine 层,UI 层只测了「app 能启动」。已补 `test/frames_screen_test.dart`(7 个用例,直接 pump 真 widget)。

### P1(已全部修)
1. **恢复购买会被静默吃掉**:`store.load()` 在 `await` **之前**取 `earlyPro`,StoreKit 的启动重放若落在读文件的 await 窗口里,`pro` 会被文件值打回 false 且不落盘 —— 用户看到「已恢复」却没解锁。改成 await 之后再取。
2. **停止后 `notifyListeners()` 打在已 dispose 的 notifier 上** → debug 下断言抛出、未捕获异步异常红屏。`StackRunner` 加 `_disposed` 闸。
3. **iOS 上的文案提到 Android**(HEIC 解码失败那条)—— PIPELINE 明记这条被苹果 2.3.10 拒过。改成不提平台。
4. **多张被跳过时只看得到最后一条原因**:`showNotice` 自带 `hideCurrentSnackBar`,循环 N 次只剩最后一条。改成合并成一条(前 3 条 + 「另有 N 张」)。
5. **「开始叠加」无防重入**:双击起两个并发 run + 两份 ~1GB 暂存,且 `WorkDirs.fresh` 同毫秒会撞同一个目录 → 互相覆盖叠出错图。加 `_starting` 闸 + 目录名加自增序号。
6. **`kMaxFramePixels = 60MP` 太宽**:108MP/200MP 手机会被 lowmemorykiller **直接杀掉(不是可捕获异常)**。压到 **24MP**,并给专属失败原因「照片像素太多」+ 可照做的建议。
7. **预览渲染失败 = 永远转圈 + 导出永久禁用且不说原因**:catch 只 `debugPrint`。改成写 `_loadError`,并把导出按钮的 enable 条件从「预览图好了」改成「原图在盘上」—— 预览失败不该挡住全分辨率导出。
8. **HEIC 转码目录跑完不删**,与商店文案「临时文件跑完就删」不符。`ImportResult` 带回 `scratchDir`,`FramesScreen.dispose` 删掉。
9. **免费用户超额被静默勾除,不弹解锁页** —— 违反 G3「每个免费额度门都要打开 showProSheet」。改成进页面即弹,说明保留了前几张;**并且在本页买了 Pro 会把被砍掉的帧自动放回来**。
10. **「停止」延迟被说成 5 秒,实际做不到**(取消点在帧边界,单帧真机 15–40 秒)。不假装能立刻停:按钮立即变「正在停止…」、文案说「当前这一张处理完就停」、进度环转黄。验收清单已改写。

### 顺带修掉的 P2
- **报告里的「检出星点」恒等于 50**(控制点上限截断)→ 改上报截断**前**的真实检出数,否则这一栏不是信息。
- **短读(磁盘写满导致 raw 被截断)补 0 会静默压黑一条横带** → 改成该帧本条不参与合成。
- 叠加的帧下标数组 `Uint8List` → `Int32List`(帧上限一旦过 255 会静默回绕叠错帧)。
- **失败原因归错类**:只有 3 对点时报的是「对齐误差过大 / 把手机架稳」,实际原因是匹配太少 —— 新增 `tooFewMatches` 与对应建议。
- **点对只按 source 去重**,两颗源星可以抢同一颗参考星 → 拉偏拟合并虚高匹配数。补上 target 侧去重。
- **RANSAC 最坏 O(n²) 恰好发生在失败帧上**(用户正等着)→ 最小样本试验封顶 250 次。
- 进度环从 100% 跳回 0%(对齐与合成换了分母)→ 全局归一化(对齐 0–70%、合成 70–100%)。
- **HEIC 方向双重旋转**:`keepExif:true` + `autoCorrectionAngle:true` 会既转像素又留 orientation 标签,下游 `bakeOrientation` 再转一次 → 整叠歪掉。加 `SourceFrame.orientationBaked` 一路传到解码处。**这一条仍须真机专门试一组 HEIC。**
- iOS 删掉 `UISupportedInterfaceOrientations~ipad` 的四方向残留;Android 加 `allowBackup="false"`(Pro 标记不进云备份,与零联网卖点一致)。
- 设置页「清理临时文件」接上真实占用字节数;补 PLAN 里承诺过、代码里漏掉的「重新叠加」入口。
- 删死代码:`AlignedOutcome` / `AlignPayload` / `fitSimilarityStars` / `coverageOf` / `AlignFailure.writeFailed`(不可达)/ `AlignResult.maxPixels` / `core/day_change.dart`。
- 英文商店标题避开 `Stacker` 词根(四支竞品已占),改 `AstroPile: Stack the Night Sky`。

### 审计确认无误的部分(记下来,免得下次重查)
并查集路径压缩、游程 8 邻接判定、除零与 NaN 防护、相似变换闭式解与其逆、**`rowCoverage` 的覆盖区间与 `warpRgb` 的越界判据语义一致(所以没有边缘发黑)**、中值插入排序、`mtf`/`midtoneFor` 互逆、自定义异常能跨 `Isolate.run` 原样传回、取消后临时目录会被删、零联网(全 lib 零网络调用)、JSON 原子写与升级兼容、`grep buyPro(` 只在 `purchase.dart` 定义处与 `pro.dart` 唯一调用处、`ui.Image` 生命周期三处 dispose 都对、**背景 isolate 里零 `tr()`**(失败一律以枚举回传,UI 侧本地化)。

### 已知未修(写清楚,别再重复排查)
- **停止延迟仍是帧粒度**(见 P1-10)。要降到亚秒得把 `_alignFrame` 拆段或上长驻 isolate + 端口 —— M2。
- **shell 的 `settings_page.dart` 带 `ja:` 串而工具层没有** → 日文系统手机上设置页是日语、其余是英文。这是**壳的问题,四个已上架 app 共有**,不在本 app 单独改(改 `l10n.dart` 的 `effectiveCode` 回落一行即可,但那是壳母版,须单独一轮)。
- `rowCoverage` 与 `warpRgb` 的边界可能差 1 ulp,极端情况下最边一列会是黑的(肉眼不可见级)。
- 每条一个 `Isolate.run`(12MP 约 63 次 spawn),可合并成一个长任务 + 端口回报进度 —— 收益是几百毫秒,M2 再说。

## G6 / G6b 出包与视觉复核(2026-09-10)

- **CI run [34440271112](https://github.com/noobclaw/laura/actions/runs/34440271112)**:`build-android` ✅ + `smoke-test` ✅(模拟器装包→启动→30s 验活→logcat 无 FATAL EXCEPTION→截屏)。
- **G6b 逐条对照「视觉设计标准」rubric**(依据该轮 `apps-astropile-smoke` 的首页截屏 + 通读冒烟截不到的四屏源码):
  - 层级与留白 ✅ hero → 主行动 → 三步说明 → 离线声明,主次一眼分明,间距走 8/12/16/20/26 的节奏;
  - 配色 ✅ 夜空渐变 hero + periwinkle 主色,语义色只用在状态(绿/黄/红 = 质量分与失败);深色方案在 `buildAstroTheme` 里单独压过表面色;
  - 字阶 ✅ headlineSmall / titleSmall / bodySmall 三级分明,数字(星点数、残差、质量分)走等宽加粗;
  - 形状与质感 ✅ 圆角 20/24/26 一致,卡片扁平填充,进度用环不是条;
  - 空状态有温度 ✅ 首屏不是一行冷字,是「它做三件事」+ 图标 + 一句主张;
  - hero ✅ 渐变 + 星芒 + 一句主打句(**楔子原文**),第一眼就说清这个 app 干什么;
  - 数据可视化 ✅ 进度环 + 逐帧结果流 + 报告页的指标组,有单位(px / °)、有色义;
  - 一致性 ✅ 五屏共用 `SectionCard` / `StatusPill` / `scoreColor` 一套语言。
  - **打回重做 1 处**:hero 的装饰星芒用 `Clip.none` 挂在圆角卡外面,窄屏上会在卡片外糊一小片白 → 改 `Clip.antiAlias`,重出包。
- **最终包 = CI run [34441460214](https://github.com/noobclaw/laura/actions/runs/34441460214)**(含上面那处修复),`build-android` ✅ + `smoke-test` ✅,产物 `apps-astropile-apk` / `apps-astropile-aab`。
  ⚠️ **这一轮的冒烟截屏被模拟器自身的 `Pixel Launcher isn't responding` 弹窗盖住**(是模拟器桌面 ANR,不是本 app —— 冒烟判定的是本 app 进程存活 + 无 FATAL EXCEPTION,两项都过,截屏里本 app 在弹窗后面渲染正常)。**故 G6b 的有效视觉证据取自上一轮截屏**,两轮之间的代码差异只有那一处 clip。

**用语纪律(G7)**:现在只能写「**🧪待验收**」——过了机器冒烟 ≠ 能用。真机核心功能(见上「真机验收清单」12 条)验过才准写「可发布」。
