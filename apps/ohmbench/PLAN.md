# 电路台 OhmBench — 立项评审(G2)

> 状态:**M1 已交付(2026-09-22 工厂轮),🧪待真机验收**。G2 审计的 1 P0 + 5 P1 已全部修复并各有回归测试;G8a 提审前审计(1 P0 / 7 P1 / 8 P2)已修 P0 + 全部 P1 + 6 个 P2。`flutter analyze` 零 issue,`flutter test` 74 条全绿。
> ⚠️ **色相改动**:原定 198°(示波器青)与 draftbook(0xFF0A6C96,同为 198°)撞带,违反 PIPELINE 视觉标准 13 → 种子色改为 **52° 电荷黄 `0xFFDCC21C`**;青色只留在画布上作为「正电压」的数据色。§十 的视觉方向据此更新。
> 本轮详细记录见 **§十三**。

---

## 一、一句话定位

手机和平板上**算得对、编得动、丢不了**的电路仿真台:画一张原理图,立刻看到每个节点的电压、每条支路的电流和示波器波形,全程离线、一次买断。

## 二、差异化楔子(「用户为什么选我而不是 iCircuit / CRUMB」)

**在位者的差评收敛到四件事,而这四件事没有一支在商店描述里承诺过。**(6 支在位者描述今日全读,见 §三)

> **别的电路 app 会跟你打架、会丢你的作品、要联网、买了还有广告;OhmBench 不会 —— 它没有网络权限,所以它做不到。**

四条腿,按**可证伪程度**排序(这是本轮对楔子的重要修正,见 §三 3):

| # | 楔子 | 证据(用户原话) | 可证伪性 |
|---|---|---|---|
| ① | **作品丢不了**:项目可重命名、可分组、无限撤销重做、杀进程后原样还在 | 「can't rename the project」「no way of renaming projects」「I need a way to build one circuit in a folder and be able to access it in another」「Craps no components at all when create」 | **强**:任何人装上就能验,且**没有任何在位者提过** |
| ② | **算得对**:每个数与 SPICE 同口径(RELTOL/VNTOL/ABSTOL/gmin 全用 SPICE 默认),不收敛就明说「未收敛」,绝不编一个数 | 「算错」类差评(跨 2011–2026);`EveryCircuit` ★2.98 | **强**:误差门限写死在测试里(§六),可复现 |
| ③ | **零网络**:没有 INTERNET 权限,飞行模式冷启动全功能 | 「the app **refuses to start without an internet connection**」 | **强**:权限清单是商店可见事实 |
| ④ | **零广告零订阅**:一次买断,免费层也不放广告 | 「Paid for app and **there's ads**」×5 / 「I'd **refund** if I could」 | **弱 —— 已被人占**,见下 |

**⚠️ ④ 的原创性今日实测被打掉一半(纪律:楔子原创性检查必须在 PLAN.md 阶段做)**:
- `iCircuit`($12.99)描述里有 **「iCircuit is a complete app with a one-time purchase.」** —— 09-19 日报记的「6 支 0 处提买断」**在 iCircuit 这一支上不成立**,须更正。
- 同开发者的 `Logic Circuit Simulator Pro`($2.99 / **★0.00 / 0 条** / ver 2025-04-22,今日新发现,**此前 7 天的枚举都没抓到它**)描述写 **「Ad-Free & Offline Mode: Enjoy an uninterrupted, premium experience without ads or connectivity limitations.」** —— **「无广告」和「离线」两个词被逐字占了**,而占它的正是那支被 5 条差评骂「付了钱还有广告、离线打不开」的 app 的兄弟版。

**结论**:④ 从「卖点」降级为「事实陈述」,**不做副标题主句**;副标题主句改用 ① + ②(没有人讲,而且讲了就必须做到)。**「说了没做到」本身是这一格的结构性机会**:我们把它做成可验证的(无网络权限 = 商店页可见;误差门限 = 测试可复现),而不是一句同样的广告词。

## 三、这一格今日实查(2026-09-19,G2 前置③)

### 1. 在位者复核(iTunes Search,`limit=40` × 4 组主词,逐行读完,纪律 BH①)

| 支 | 价格 | ★/评价数 | 当前版本 | 用途 |
|---|---|---|---|---|
| `CRUMB` | $7.99 | 4.37 / 140 | **2022-08-29(停更 4 年)** | 面包板仿真 |
| `iCircuit` | $12.99 | **3.81** / 134 | 2026-03-18(在维护) | 模拟+数字 |
| `iCircuit 3D` | $12.99 | 4.11 / 72 | 2026-02-28 | 3D 面包板 |
| `EveryCircuit` | Free + $14.99 解锁 | **2.98** / 65 | 2023-04-09(停更 2.4 年) | 动画仿真 |
| `Logic Circuit Simulator` | $2.99 | **3.39** / 46 | 2025-02-26 | 数字逻辑 |
| `Breadpad` | Free + $40/年 | **3.95** / 158 | 2026-09-12(在维护) | SPICE + iCloud |
| `Digital Gate Circuit Simulator` | $7.99 | **3.64** / 11 | 2019-11-28(停更 6 年) | **🆕 第 7 支,今日新增** |
| `Logic Circuit Simulator Pro` | $2.99 | 0.00 / **0** | 2025-04-22 | **🆕 今日新增,按纪律 R(<10 条)不计入付费面** |

### 2. 免费侧今日补全(此前只记了 `VoltSim`)

`Circuit Design 3D Simulator` ★**3.97**/198(2025-06-04)、`Circuitry - 3D Circuit Builder` ★**3.87**/156(2023-11-13,停更)、`Eureka Electronics` ★**2.51**/223(2026-08-12,在维护)、`Digital Circuit Simulator Lite` ★**2.90**/30、`VoltSim` ★4.67/81(2026-08-11)。

**→ 三条读数**:① 免费侧最大评价面 **223 条**,**没有任何一支接近 10 万** → 09-13 硬条款②「不和巨头竞争」**仍然通过**;② 免费侧新补的三支**全部 ★<4.0**,缺口科的方向**被加强而不是削弱**;③ 唯一健康的免费同类是 `VoltSim`(★4.67 但只有 81 条)—— 它是这一格**唯一需要认真对待的对手**,M1 上架前须再查一次它的更新记录。

### 3. G2 前置③「装机核验 VoltSim / Breadpad 是否已修掉触屏编辑问题」——**只做到一半,记缺测**

能做的:商店侧版本与描述扫描(已做,见上)。**做不到的**:本机没有 iOS 设备,`VoltSim`/`Breadpad` 的触屏编辑手感**未实测**。
**处置**:不写成「已核验」。**M1 提审前必须补这一条**(用户真机装这两支各 10 分钟,按 §五 的 8 条操作逐条打分),否则缺口科的「触屏编辑打架」这条腿只有 2016–2026 的差评,没有当下的实证。

## 四、架构评审(招牌功能的可靠性机制,写码前定死)

| 机制 | 选型 | 为什么不是另一种 |
|---|---|---|
| 数值引擎 | **改进节点法(MNA)+ 牛顿迭代 + 梯形积分**,稠密 LU + 部分选主元 | 手机电路规模是几十个未知数,稠密 n³/3 在 n=60 约 7 万次浮点 = 远低于一帧;稀疏排序(SpiceSharp 的 Markowitz)留到 M3 且只在实测需要时做 |
| 非线性收敛 | **限压(`DEVpnjlim`)+ gmin 阶梯 + 源阶梯**三级兜底,全用 SPICE 默认容差 | 只有牛顿 = 二极管一次坏猜测就 `exp` 溢出;三级兜底是 SPICE 四十年的答案,不自创 |
| 拓扑→网表 | **并查集**,以「点落在线段上即连通」为规则(T 型接点自动成立) | 只认端点相连 = 用户必须精确命中端点,正是「触屏编辑打架」的病灶 |
| 坐标 | **整数栅格**,落点即吸附,绝不存浮点坐标 | 浮点坐标 = 「看起来连上了其实差 0.3 格」,这类静默失败最难被用户理解 |
| 撤销 | **整文档快照栈**(上限 512 步) | 逆操作栈是「撤销把电路搞坏了」这类 bug 的老家;一张手机尺寸的图几 KB,快照更便宜也更安全 |
| 存盘 | 壳的 `JsonFileStore`:临时文件 + 原子 rename;解析失败**改名留档**不覆盖 | 用户唯一一份作品,不接受「写一半崩了」 |
| 实时仿真 | 开关用**电导**建模(闭合 1mΩ / 断开 1TΩ),拨动不改变矩阵结构 | 改结构 = 每次拨开关重建矩阵并可能丢收敛历史 |
| 网络 | **无 INTERNET 权限**(壳默认 `tools:node="remove"`) | 楔子③ 就是这条;有权限就不可证伪 |

**服务端:无。** 本项不使用 `256btc.com`,无同步、无云端、无账号 → 离线降级问题不存在,App Store 数据收集声明填「不收集」。

**危险权限:无。** 不用相机/麦克风/定位/通知。iOS 侧因此**不需要任何 `NS*UsageDescription`**,也**不得**提交 `ios/Podfile`(PIPELINE iOS 铁律②)。

**iPad:M1 不发。** 本候选在 BACKLOG 里写的是「iPhone/iPad 电路仿真」,且 Reddit 信号最新一条来自 `r/iPadPro`,`CRUMB`/`iCircuit 3D` 都是 iPad 主场。但按 PIPELINE「iOS 只发 iPhone(`TARGETED_DEVICE_FAMILY = 1`)」,发 iPad 就必须另做布局 + 13 吋截图,拉伸充数会撞 2.3.3。**决定:M1 iPhone-only,iPad 作为 M2 的第一项**(那时画布/工具栏要重新排版,不是加个 flag)。此事**写进 M2 验收**,别在 M1 悄悄打开。

## 五、M1 验收表(8 条,每条都绑一句用户原话)

前 6 条来自 09-17 入队评审,第 7、8 条来自 09-19 新读的 16 条差评。**M1 交付时这 8 条逐条演示,缺一条不算完成。**

| # | 用户原话 | 验收标准 | spike 现状 |
|---|---|---|---|
| 1 | 「Once on the breadboard, **moving them seems impossible**」 | 放置后可拖动、可原地旋转;旋转绕自身原点,不走位 | ✅ 模型层已过(`editing_test.dart` 验收 1) |
| 2 | 「can't find menu tools to add anything」 | 从**已有引脚**直接拉线;点引脚优先于点元件体 | ✅ 命中测试已过(验收 2) |
| 3 | 「no clear or concise interactions」 | 误触保护:位移不过 12pt 一律算点选,文档零改动 | ✅ 状态机已过(验收 3) |
| 4 | 「**moving them seems impossible**」(整体) | 框选多个元件+导线整体移动,连通性不变 | ✅ 已过(验收 4) |
| 5 | 「I feel like I am **fighting the program**」 | 撤销/重做无实际步数上限(512 步内存闸),按钮显示将撤销什么 | ✅ 已过(验收 5) |
| 6 | 「**Craps no components at all when create**」 | 杀进程后作品原样还在;文件损坏不吞数据 | ✅ 序列化往返已过(`document_test.dart`);UI 层存盘留到 M1 |
| 7 | 「the app **refuses to start without an internet connection**」 | **飞行模式冷启动全功能**;`Info.plist`/Manifest 无任何网络声明 | ⬜ M1 装机实测 |
| 8 | 「Paid for app and **there's ads**」×5 | 零广告、零追踪 SDK、零账号;一次买断 | ⬜ M1(壳本来就没有广告 SDK) |

## 六、引擎误差门限(写死在测试里,G2 已达成)

`flutter test` 50 条全绿(引擎 22 / 图纸 27 / 壳 1)。门限与**今日实测值**:

| 场景 | 门限 | 实测 | 对照方法 |
|---|---|---|---|
| 线性 DC(分压/并联/电桥/串联源) | 相对 1e-9 | 精确到 1e-12 | 手算闭式解 |
| 二极管工作点(电压) | ≤ **1e-5 V** | **3.4e-6 V** | **独立二分法**解同一 Shockley 方程(与被测代码零共用) |
| 二极管电流 | ≤ 1e-3 相对 | 1.3e-4 | 同上(电流对电压指数敏感,dI/I = dV/Vte) |
| RC / RL 阶跃(h = τ/100) | 摆幅的 0.5% | <0.5% | 解析 `1-e^{-t/τ}` |
| LC 谐振(3 个周期) | 幅度漂移 ≤1%、周期误差 ≤0.5% | 均满足 | 解析 `1/(2π√LC)` |
| 半波整流 / 二极管钳位 | 正峰 = 输入 −0.7V ±0.25V;负半周不跟随 | 满足 | 定性 + 边界 |
| 不收敛 / 奇异 | **一个数都不报**,返回 `didNotConverge` / `singular` | 满足 | 两源并联不同电压 |
| 悬空元件 | 仍出解,并标 `floatingNodesTiedToGround` | 满足 | 1GΩ 对地兜底 |

**⚠️ 仍欠一条(M2 补):与 `ngspice` 的交叉对账。** 现在的对照全是闭式解与独立二分法 —— 已经能挡住算错,但**挡不住「模型选错」**(例如二极管温度系数、电容初值语义)。M2 要在开发机装 `ngspice`,导出 `.cir` 跑同一批电路,把标准答案抄进测试。

## 七、参考实现分析(G8c 的底稿)

详见 `REFERENCE.md`(函数级清单 + 逐条对照表)。摘要:

| 仓库 | 许可证 | 用法 |
|---|---|---|
| `SpiceSharp/SpiceSharp` | **MIT** | **可直接移植 + 署名**。已移植:收敛判据、`DEVpnjlim` 限压、二极管 DC 加载、gmin/源阶梯、梯形系数。署名写进「关于」页与 listing |
| `sharpie7/circuitjs1` | **GPL-2.0** | **只读行为、不复制代码**。只借「栅格吸附 / 抓取半径 / 拖拽模式」的**交互规格**,并按触屏重新定值(它的 `POSTGRABSQ=25`px² 是鼠标口径,对手指太小 → 我们用 22pt 半径、随缩放换算) |
| `tilk/digitaljs` | BSD-2 | M3 数字逻辑用,本轮未读 |

## 八、里程碑(大型项按 CLAUDE.md 放宽条款③,一轮工厂一个)

### M1(下一轮工厂,可上架)
- 元件:电阻/电容/电感/二极管/直流源/正弦源/脉冲源/开关/地/导线(引擎已支持全部)
- 分析:工作点 + 瞬态(固定步长);探针读电压/电流;单通道示波器
- 编辑器:8 条验收操作全通;栅格画布 + 双指缩放平移
- 文档:多文档列表、**重命名**、复制、删除;自动保存
- 免费层:**单图 12 个元件 + 保存 1 份**(够画分压/RC/半波整流,真可用);Pro 解锁无限
- signature 场景:**电流沿导线流动的电荷点 + 节点电压染色**(`CustomPainter` + `AnimationController`,随解出的电流改变点速与密度;「减少动态效果」下降级为静态染色)
- 评价弹窗触发点:**第 3 次成功跑完一次仿真**

### M2
- 步长控制(局部截断误差 + 断点)、AC 扫频(波特图)、双通道示波器与游标
- 元件:BJT / MOSFET / 运放 / 受控源;子电路
- **iPad 布局 + 13 吋截图**(见 §四)
- `ngspice` 交叉对账进测试

### M3
- 数字逻辑(参考 `digitaljs` 规格)、参数扫描、网表导入导出(LTspice `.asc` / SPICE `.cir`)
- 稀疏矩阵(仅当实测需要)

## 九、定价与变现

- **免费 + Pro 一次性买断 $4.99**(中区 ¥28)。锚定:对标 `iCircuit` $12.99 的 **1/3~1/2**(PIPELINE 定价阶梯:标准工具 $2.99–4.99 上沿,因为这一格付费带是 $2.99–$14.99 且用户明确为「无广告无订阅」付钱)
- 商品 ID:`com.noobclaw.ohmbench.pro_unlock`(App Store 商品 ID 全账号唯一)
- 免费层门:元件数与文档数,**不是功能残废** —— 仿真精度、示波器、撤销、存盘全开
- **禁广告**(楔子③④ 的前提);设置页必须有「恢复购买」;价格一律走 `ProPriceText`,不硬编码
- 价值预估(非承诺):$4.99 × 保守 15~60 次/月 ≈ **$75~300/月**。⚖️ 这一格最大的付费面只有 140 条评价 / 16 年,按「验证品」而非「收入品」计

## 十、视觉方向(G3 照做,G6b 照查)

- ~~色相 198°(示波器青)~~ → **52° 电荷黄 `0xFFDCC21C`**(2026-09-22:198° 与 draftbook 撞带;黄带 50–65° 为空档)。画布数据色:正电压青 `#3FD4F7`、负电压品红 `#FF5C93`、电荷点暖黄 `#FFD66B`
- art direction:**深色优先的实验台**——深墨底 + 青色发光走线,元件是细线条白描,不是 3D 拟物(`CRUMB`/`iCircuit 3D` 都是 3D,正面撞不划算也没必要)
- hero 不是「大圆按钮」:hero 是**画布本身**,顶部一条细工具带,右下角一个跑/停的悬浮键
- 自绘图形语言:栅格、导线电荷点、节点电压色标、示波器网格与游标 —— 至少四处 `CustomPainter`
- 空状态:一张**半透明的示例电路**(分压器)浮在画布上,一句「点任意元件开始,或载入这个示例」

## 十·五、设计简报(2026-09-25,按 `kb/UIUX规矩.md` 5.2 填写;替代 §十 中与之冲突的描述)

## 设计简报 — OhmBench/电路台
1. **一句话**:学生、创客在实验桌边或课间,单手持机(编辑时偶尔双指缩放),要在 **3 秒内看到一个电路真的在跑**,1 分钟内搭出并跑通自己的分压/RC/整流。
2. **首屏是什么**:一块满幅的**运行中实验台**——整流电路按真实解算结果染色、电荷点沿导线流动、下方一条示波器带实时描出电容电压,右上角等宽数字读数跟着游标跳。它不是卡片,没有圆角外框,点阵栅格铺到屏幕边。其下是「我的电路」(发丝线分隔的行,不是卡片)和「示例」(2 列小图纸)。**主操作在哪里**:屏幕底部 0–12% 高度的**固定工具条**(拇指区):右侧电荷黄「新电路」主按钮、左侧「设置」;点实验台本身 = 打开这块板并直接运行。
3. **3 秒首胜(F1)**:冷启动 **0 次点击**即看到整流电路在跑(信号、电荷、示波器都是真解算);**第 1 次点击**(点实验台或任一示例)进入编辑器且**已自动开始运行**。预填数据 = 内置 4 个示例(整流/RC/分压/LC),不需要注册、引导页或任何权限。
4. **原型**:蓝图工作台 Blueprint;与它共用原型的已有 app:**无**(本 app 即 2.1 表中的占用者);最接近的是仪器面板 tunekit——字体不同(我:Martian Mono 等宽工程字;tunekit:无打包字体/将来 JetBrains Mono·Chivo Mono),形状不同(我:0/4/胶囊三档、发丝线分隔、点阵栅格;tunekit:刻度盘+指针)。
5. **色彩**:策略 **Committed**(深板底 + 线稿色,强调色只给主操作与电荷);色相 **52° 电荷黄**(色相表 50–65 空档,不撞;青色 192° 只作画布上的「正电压」数据色,不进 chrome)。
   - 浅色:**不做**(dark-only,理由见下「深浅色决定」)
   - 深色:bg `#0A1016` · surface `#111C25` · ink `#E4EDF2` · accent `#FFD66B`(电荷黄,onAccent `#2B2100`)· signal `#3FD4F7`(正电压)/ `#FF5C93`(负电压);语义色另列:warning `#FFB454`、danger `#FF6B6B`(与品牌黄分开)
   - **深浅色决定**:保持 dark-only。①画布是示波器屏,颜色**本身就是数据**(电压→青/品红、电流→电荷点亮度),这套编码只在暗底上可读,浅底上发光电荷点失去意义,等于要重做一套数据编码;②使用场景是实验桌/实验室,真实示波器与万用表屏一律暗底;③一个 app 一张脸——库页、编辑器、面板同一块板,避免「白色外壳里嵌一块黑屏」。代价:偏离 F13「非 Nocturne 须跟随系统」,已列入未决项给用户拍板。深色底用带色相的深墨(非纯黑),正文 90% 白;inkDim 对 bg 7.3:1、对 surface 6.6:1(测试 `test/ui/design_tokens_test.dart` 实算)。
6. **字体**:展示字 **Martian Mono**(OFL-1.1,无保留字体名;来源 `github.com/google/fonts/ofl/martianmono` 可变字体 → fontTools 实例化 wdth=87.5(窄体,工程图注字的比例)并子集到 Latin-1+常用符号)`assets/fonts/MartianMono-Regular.ttf`(400,22.9 KB)、`MartianMono-Bold.ttf`(700,23.2 KB),许可证随包 `MartianMono-OFL.txt` 并在 `LicenseRegistry` 注册。只用在**字标、读数、数值、元件标注、区块标题**——这些本来就是测量/工程图的字,等宽 = 数字天然对齐(另加 `tabularFigures`)。中文展示子集:**不用**(中文走系统苹方)。正文 = 系统字体。回退链:`Martian Mono → PingFang SC → Heiti SC → Noto Sans CJK SC → Roboto`(Ω 等字库外符号由回退承担)。
7. **形状**:3 个圆角值 **0 / 4 / 胶囊**(`BenchRadius.none/sm/pill`);分隔方式 = **发丝描边**(1px `#223340`),不用阴影与渐变卡;基准间距 **4pt**(常用 4/8/12/16/24);行高 **44pt**(仪器/蓝图档)。
8. **图形语言**:**点阵栅格 + 按电压染色的导线 + 沿导线流动的电荷点**;显示的真实数据 = 每个节点的解算电压(颜色)、每条支路的解算电流(电荷点速度与方向)、电容节点电压随时间(示波器带 + 右上角读数)。
9. **signature 场景**:首屏实验台上的半波整流在跑——正弦源一侧导线随电压青/品红呼吸、二极管导通时电荷涌过、电容两端锯齿波在示波器带上被游标追着描;由 **isolate/同步求解的瞬态结果**驱动(`simulateNow` → `SimRun` 节点电压/支路电流逐帧播放)。开启「减少动态效果」后:停在**稳态最后一帧**——颜色、示波器整条波形与读数照常,电荷点静止,游标定在末端(`LivePreview` 不起 Ticker)。
10. **空状态**:库里没有电路时,在「我的电路」位置画一张 **20% 不透明的分压器幽灵图**(同一个 `SchematicPainter`,不是图标圆)+ 一句「第一张电路会出现在这里」+ 行内「空白画布」按钮(拇指区底部工具条还有「新电路」);编辑器空画布:中心画一个幽灵电阻 + 「从下方选一个元件开始」,箭头朝下指向元件条。
11. **触感映射**(统一封装 `BenchHaptics`,设置页总开关,禁 heavy):① 运行/停止 = `mediumImpact`;② 提交类(放下元件、连好一根线、拖动落位、存为我的电路、删除)= `lightImpact`;③ 选择类(运行中点探针、长按起框选、撤销/重做、切连线模式、拨开关)= `selectionClick`;拖动途中逐格**不再**震动(一次手势只在落位时震一次)。
12. **首屏线框**
```
┌──────────────────────────────────┐
│ ⌇ OhmBench              3.21 V   │ ← 字标 + 实时读数(Martian Mono)
│ ·  ·  ·  ·  ·  ·  ·  ·  ·  ·  ·  │
│  ~ ━━━▷|━━●━━━━━━┓ ·  ·  ·  ·    │ 满幅运行中实验台(点它=打开并运行)
│  ┃  ·  ·  ·  ═╪═   ⌇  ·  ·  ·    │
│  ┗━━━━━━━━━┻━━━━━┛ ·  ·  ·  ·    │
│ ╱╲__╱╲__╱╲__ 示波器带 ___|___    │
│ 画出来,跑起来  整流·5V 50Hz→47µF │ ← 一行说明(正文)
│─────────────────────────────────│
│ 我的电路                  1 / 1  │ ← 标题(Martian Mono)+ 额度
│ [▦图] LC 振荡   5 元件·刚刚   ⋯  │ 发丝线分隔行
│─────────────────────────────────│
│ 示例   [▦ 分压] [▦ RC] …          │ 2 列小图纸
│══════════════════════════════════│
│ (⚙)                  [ + 新电路 ]│ ← 固定底部工具条(拇指区)
└──────────────────────────────────┘
```
13. **自检**:「换一个类似需求的 app 会不会做成一样?」——会撞的是「顶栏 + 渐变 hero 卡 + 卡片列表」这副壳骨架(09-23 版本身就是这样:AppBar+齿轮+圆角 26 渐变卡+Card 列表)。本轮改掉:去 AppBar 与右上齿轮、hero 由卡片改为满幅画布、列表由 Card 改为发丝线行、主操作下沉到底部工具条、字体换 Martian Mono、圆角收成 0/4/胶囊。

**Token 落点**:`lib/tool/ui/lab_theme.dart`(`Bench` 颜色、`BenchRadius`、`BenchSpace`、`BenchType`、`BenchMotion`,以及手写 `ColorScheme` + 组件主题 + iOS Cupertino 转场 + `NoSplash`);触感 `lib/tool/ui/haptics.dart`。原始 `Color(0x…)` / `circular(n)` / 动效时长与曲线只允许出现在 `lab_theme.dart`;非视觉的计时(存盘合并 350ms、编辑后重跑防抖、商店重试退避)不属于动效 token,留在各自逻辑里。

**动效时长/曲线 token**:press 120ms、small 200ms、medium 300ms、sheet 400ms、hero 600ms(退出 ×0.7);曲线 enter `Cubic(0.23,1,0.32,1)`、move `Cubic(0.77,0,0.175,1)`、sheet `Cubic(0.32,0.72,0,1)`;全部经 `BenchMotion.of(context, d)` 在「减少动态效果」下收成 `Duration.zero`,循环动画(字标电荷、运行键脉冲、首屏播放)停在静态帧。

**本轮落地与验证(2026-09-25)**:去 AppBar+齿轮,首屏=满幅运行中实验台+发丝线行+底部工具条;示例/首屏点开即自动运行(F1 ≤1 次点击);撤销/重做/连线模式下沉到元件条顶行(拇指区),顶栏只留返回/名称/更多;运行/停止键自绘字形,停止态不用错误红;删除改 Undo SnackBar;页面转场 iOS 保留 Cupertino 右滑,减少动态效果时 ≤150ms 淡入;hero 滚出视口停 Ticker。独立评审(5.3)首评 79/100、无否决项,15 条意见已修。`flutter analyze` 0 issue,`flutter test` 96 条全绿(原 74 条未删改断言;新增 `test/ui/design_tokens_test.dart` 对比度/原始值 grep/触感封装/字体打包/2.0 字号/触控与标签 guideline,及 F1 自动运行测试)。截图:`screens_test/out/1_…9_*.png`,商店图已刷新到 `store/screenshots/`。

## 十一、商店与 ASO(G8b 的底稿,提审前再实打一次)

- 英文名 `OhmBench` 今日实查美区:**零逐字同名**(`OhmBench`/`Ohm Bench`/`ohmbench` 三种写法 0 命中同名支)
- 标题(≤30)候选:`Circuit Simulator Lab – OhmBench`;副标题(≤30)候选:`Offline SPICE. Nothing lost.`
- 四句主打(副标题池):`No account. No cloud. No subscription. **No ads.**` —— ⚠️ 按 §二 的更正,**后两句已被同格产品的描述占用**,主句改用 ①② 两条(作品丢不了 / 算得对),这四句只放长描述
- 关键词与主词实打留到 G8b(`store/aso.md`),但**头部评价面已知 < 5,000 条**,属于该做的词

## 十二、本轮未做 / 下一轮必须先做

1. **M1 全部 UI**(画布、工具带、示波器、文档列表)—— 本轮只有模型层
2. **专属图标**(1024 源图 + Android 5 档 + iOS 全尺寸 + 512/1024×500 商店图)
2b. **iOS 显示名本地化**:`ios/Runner/{en,zh-Hans}.lproj/InfoPlist.strings` 两个文件 + `project.pbxproj` 注册(范本:`apps/draftbook/ios`)。本轮**故意没做** —— 改 `project.pbxproj` 而本机又无法构建 iOS 验证,风险大于收益;安卓侧的中文名(`values-zh/strings.xml` = 电路台)本轮已落
3. **`store/listing.md` 的完整中英双语正文** —— 本轮只落了草稿骨架
4. **真机核验 `VoltSim` / `Breadpad` 的触屏手感**(§三 3,缺测)
5. **需求侧 Reddit 腿仍是缺测**:`电路台` 的 ≥3 条独立原话**全部来自 App Store 差评**,`r/ElectricalEngineering` 等三个 sub 连续两天 429 / 0 命中。按 09-13 硬条款④ 这条腿要补上;补不上就在 BACKLOG 里长期挂「需求面只有商店证据」


## 十三、M1 交付记录(2026-09-22 工厂轮)

### 1. 本轮做了什么
- **引擎修复(AUDIT-G2 全部 P0/P1 + P2-7/8/10/11/12/14/15)**:主元改相对判据(对原始列尺度 1e-12,与 SpiceSharp 的 1e-3「选主元」阈值用途不同,已在代码注释写明有意偏离);悬空兜底电导改为 min(1e-9, 最小真实电导×1e-6);牛顿 `iteration>1` 守卫;限压否决收敛 + 二极管器件级收敛检验(ngspice `DIOconvTest`);0Ω 统一常量 `kMinimumOhms`;`shortedPartIds` / `invalidPartIds` 网表边界诊断;`gminSteps:0` 关闭 gmin 阶梯(ngspice 语义);阶梯保留 shunt;t=0 用 `at(0)`;拖拽起步不跳格;`copyWith(resetValue)`;JSON 非有限值守卫。回归测试 `test/engine/audit_regressions_test.dart`(15 条)+ `test/schematic/audit_regressions_test.dart`(9 条)。
- **M1 UI**:库页(运行中的整流电路 hero = signature 场景之一、我的电路、4 个示例)、编辑器(栅格画布、单指平移/双指缩放、从引脚拉线、连线模式、长按框选/按住拖动、选择操作条、数值面板支持 4k7/10u/2.2meg)、运行/停止(isolate 求解,首次从静止 uic 起跑、失败退回工作点)、**signature:导线按电压染色 + 电荷点沿导线流动**(每个节点内按导线长度解一个小 KCL 问题分配支路电流,`lib/tool/sim/flow.dart`)、**运行中拨开关从当前状态续跑**(电容电压/电感电流作初值、正弦相位续接)、单通道示波器(慢放倍数标注)、点导线测电压/点元件测电流、自动保存 + 后台 flush + 存储异常横幅。
- 免费层:1 张电路、每张 12 元件(接地不算);Pro $4.99 解除。评价弹窗触发点:**用户主动按下运行且成功第 3 次**(编辑后的自动重跑不计)。
- 专属图标(`scripts/icons.mjs` 的 `ohmbench` 条目)、iOS 中文显示名(移植 draftbook 的 pbxproj 本地化段)、竖屏锁定、Manifest 剥 INTERNET、`store/listing.md` 中英定稿。
- 本地截图工具 `screens_test/`(用 flutter test 渲染真字体截图,不进 CI)。

### 1b. 出包(G6)
- CI run **35688288956**(commit `5aa3e71`):build-android ✅、smoke-test ✅。产物 `apps-ohmbench-apk` / `apps-ohmbench-aab`。
- smoke 截屏:库页真渲染(hero 整流电路 + 电荷点、电荷黄「新电路」按钮、示例卡);截屏上的「Pixel Launcher isn't responding」是模拟器系统桌面的 ANR,不是本 app。
- G6b:本地 `screens_test` 真字体截图(库页中英 × 深浅、4 个示例的编辑器静止/运行、空画布)逐屏看过:层级清楚、hero 有动效、深色实验台与示波器质感统一;已据此改过「适配屏幕」给运行键留位、连线模式图标换 `cable`。

### 2. 真机验收清单(给用户照着点)
| # | 操作 | 期望 |
|---|---|---|
| 1 | 飞行模式冷启动 | 库页 hero 电路在动,示例可打开(验收 7) |
| 2 | 新电路 → 点「电阻」→ 拖动它 | 放在屏幕中央,拖动贴格、不跳格(验收 1、3) |
| 3 | 从电阻引脚拖到空白处 | 出现 L 形导线(验收 2) |
| 4 | 空白处长按拖框 → 拖动选中的一组 | 整组移动,连接不断(验收 4) |
| 5 | 连续撤销/重做,长按撤销键 | 提示「撤销:移动 1 项」之类(验收 5) |
| 6 | 编辑后立刻上划杀进程再打开 | 电路原样还在(验收 6) |
| 7 | 打开「RC 充电」→ 运行 → 点开关 | 电容电压在示波器上按 τ=0.1 s 上升,电荷点流动 |
| 8 | 运行「半波整流」,点各导线/元件 | 示波器切换电压/电流,读数跟随游标 |
| 9 | 删掉接地再运行 | 提示「缺少接地」,不出任何数 |
| 10 | 放第 13 个元件 / 新建第 2 张电路 | 弹 Pro 解锁页(不直接拉起支付),恢复购买可达 |
| 11 | 双指缩放中抬起一指 | 不会误拖元件或画线 |
| 12 | 系统「减少动态效果」打开 | 不再有电荷点动画,颜色与示波器照常 |

### 3. 仍欠(M1 提审前)
- G8b:`store/aso.md` 主词实打;截图 5 张中英两套。
- G8c:REFERENCE.md 按本轮引擎改动更新对照表(主元判据「有意偏离」、`DIOconvTest` 已补)。
- §三 3 缺测:VoltSim / Breadpad 触屏手感真机核验。
- G8a 剩余 P2:按住后直接拖未选中的元件、恢复购买固定 5s 等待、免费用户点示例直接进付费页(文案已说明)。
- iOS 真机未测;CI 只证明 Android 能装能启动。
