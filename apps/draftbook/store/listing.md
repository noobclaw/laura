# Draftbook — 商店文案

> ⚠️ **本文件写的是 v1.0(M1)实际交付的功能。** PIPELINE G8a ⑧:商店文案里的每一条卖点都必须在包里真的存在且可达。
> M1 **不含同步、不含账号、不含资料板** —— 那是 M2/M3。原立项时写的那版文案(带 sync / cloud history / research board)已挪到本文件末尾的「§ 附:M2 上线后再启用的段落」,**在同步真的发出去之前不得使用**。
> ⚠️ **提交前必须先建 English (U.S.) 本地化并填全**(铁律 `feedback_store_listing_must_have_enus`)。
> ⚠️ 措辞纪律:**正文与截图文案里不得出现「取代 Scrivener」「Scrivener alternative」等指名竞品的表述**(审核 + 商标风险)。
> 关键词字段里放 `scrivener` 按 PIPELINE G8b 第 2 条(「关键词 = 长尾 + 竞品名」)执行,**与已上架的 autosnore(`snorelab,shuteye`)、goldenscout(`photopills`)同口径**;
> 09-16 审计对此提过 5.2.1 商标风险,**保留是一个有意识的选择,不是疏漏** —— 若苹果就此驳回元数据,直接删掉这一个词重交即可(元数据驳回不影响二进制)。
> **`nanowrimo` 已按同一次审计删除**:它是注册机构商标、不是我们要抢的竞品词,风险高而收益低,换成 `longform`。

---

## English (U.S.) — 主语言

**Name (30)**
```
Draftbook: Novel Writing
```

**Subtitle (30)**
```
Chapters, outline & drafts
```

**Promotional Text (170)**
```
Write your book on the phone you already carry. Chapters and scenes you can drag into place, a word goal that keeps you honest, and every draft kept — all of it offline.
```

**Description**
```
Draftbook is a writing app for long work — novels, non-fiction, scripts — built for the phone you actually carry.

STRUCTURE, NOT ONE LONG PAGE
A book is a project. A project has chapters. A chapter has scenes. Drag scenes to reorder them, move them between chapters, and see the whole book as one outline you can rearrange in a minute.

WRITE WITHOUT FIGHTING THE APP
The formatting bar above the keyboard is always there and always works. No dialogs interrupt your writing. Tap Keep writing and you are back in the scene you left, keyboard up, cursor exactly where you stopped — one tap, every time.

NOTHING YOU WROTE IS EVER LOST
Every scene keeps its last 20 versions on the device. One is taken when you first change the text and another when you leave, so a bad afternoon is a couple of taps from being undone — and restoring is itself undoable, because the text it replaces is saved first.

NO ACCOUNT, NO CLOUD, NO NETWORK
Draftbook works entirely offline and makes no network requests of any kind. Your manuscript is stored on this device — and nowhere but your own device backup. Your work only leaves the app when you export it yourself.

WORD GOALS THAT MEAN SOMETHING
A daily goal, a streak, and the last two weeks as a chart. Words cut count as cut, so the number is the truth rather than a pat on the head.

EXPORT WHEN YOU'RE READY
Send the whole manuscript out as Markdown or plain text, with your chapter titles in place. Pro adds Word (.docx) with a page break per chapter and real heading styles.

FREE
- One book, unlimited words, chapters and scenes
- The draggable outline
- Daily word goal and streak
- Version history for every scene
- Markdown and TXT export

PRO — ONE PAYMENT, NO SUBSCRIPTION
- Unlimited books
- Word (.docx) export

One purchase, no account, no monthly fee.
```

**Keywords (100, no spaces — 98 chars)**
```
scrivener,manuscript,corkboard,scene,writer,author,book,wordcount,longform,storyboard,offline,plot
```
> 标题已含 `novel` / `writing`,副标题已含 `chapters` / `outline` / `drafts` —— 苹果自动合并,关键词字段里不再重复(重复是浪费额度)。

**What's New (v1.0)**
```
First release. Chapters, scenes, a drag-and-drop outline, a formatting bar that is always there, daily word goals, version history for every scene, and Markdown/TXT export — all working with no account and no network.
```

---

## 简体中文（不发中国大陆区，仍须填）

**名称**
```
Draftbook 写长篇
```
> ⚠️ 名称里刻意不含「写作」「码字」等中区在位者可能逐字同名的词（09-13 `倒数日` 撞上 270 万条评价免费龙头的教训）。

**副标题**
```
章节 · 场景 · 大纲重排
```

**描述**
```
Draftbook 是给长篇写作用的 —— 小说、长稿、剧本 —— 为你随身带的手机做的。

有结构，不是一整页
一本书是一个项目，项目分章，章分场景。拖动场景就能重排，也能跨章移动；整本书的场景一屏铺开，一分钟就能重新排好顺序。

写的时候不跟 app 较劲
键盘上方的工具条随时都在、随时能点。写作中不弹任何窗。点一下「接着写」，就回到上次那个场景，键盘弹起，光标停在你停笔的地方 —— 一次点击，每次都是。

写过的字不会丢
每个场景各自留最近 20 个版本，存在本机。开始改动时留一版、离开编辑器再留一版；写砸的那个下午，两三下就能撤回 —— 而且「恢复」本身也能再撤回，因为被替换掉的那一版会先被存起来。

没有账号，没有云，不联网
Draftbook 全程离线，不联网，没有任何网络请求。稿子只存在这台设备上（以及你自己的系统备份里），不去别的地方。只有你自己点导出，它才会出去。

字数目标是真的
每日目标、连续天数，以及最近两周的柱状图。删掉的字会照实扣回去 —— 这个数字是实话，不是哄你开心。

写完导得出去
整本稿子可以导出为 Markdown 或纯文本，章节标题都在。Pro 再加一个 Word（.docx）：按章分页，标题带大纲级别。

免费包含
- 1 本书，字数、章数、场景数都不限
- 可拖拽的大纲
- 每日字数目标与连续天数
- 每个场景的版本历史
- Markdown / TXT 导出

Pro —— 一次买断，不订阅
- 项目数量不限
- Word（.docx）导出

买一次就好，没有账号，也没有月费。
```

**关键词**
```
长篇,小说,大纲,章节,场景,书稿,作者,码字,字数,离线,导出,版本
```

**宣传文本（170）**
```
在你随身带的手机上写你的书。章和场景随手拖、每日字数目标不哄人、写过的每一稿都留着 —— 全程离线，没有账号。
```

**What's New（v1.0）**
```
首个版本。章节与场景、可拖拽的大纲、始终在位的格式工具条、每日字数目标、每个场景的版本历史，以及 Markdown / TXT 导出 —— 不需要账号，也不联网。
```

---

## 截图脚本(5 张,与 `screens_test/store_shots_test.dart` 一致;成品在 `store/screenshots/{appstore,play}/{en,zh}/1-5.png`)

| # | 画面 | 标题 / 副标题(EN) | 标题 / 副标题(ZH) |
|---|---|---|---|
| 1 | 首页:校样页上的最后几行 + 行规尺 + 「接着写」 | Pick up mid-sentence / Open it and your last lines are there. One tap, and so is the caret. | 从停笔那一句接着写 / 打开就是你的稿子，点一下，光标回到原处 |
| 2 | 编辑器全屏,底部格式工具条 | A quiet page to write on / Saved at every pause. The toolbar never leaves your thumb. | 只有稿子的书写页 / 每次停顿都自动保存，工具条一直在拇指下 |
| 3 | 大纲:章与场景,拖动柄在行尾 | The whole book on one screen / Chapters and scenes, dragged into the order the story wants. | 整本书一屏看完 / 章与场景，拖一下就换成故事要的顺序 |
| 4 | 版本历史时间线 + 版本预览 | Every draft is kept / Twenty versions of every scene. Restore any of them, and undo that too. | 写过的每一稿都在 / 每个场景留 20 版，随时换回，也能撤销 |
| 5 | 写作统计面板:行规尺 + 最近两周 | Today, set in ink / Your daily goal on a typesetter's line gauge. No account, no cloud. | 今日字数，一眼看见 / 每日目标画在排字行规上；无账号，不联网 |

> 截图用 `flutter test screens_test/store_shots_test.dart` 渲染真实界面,改了界面或文案就重跑,不手工拍。

## Google Play 专用补充(App Store 不用)

- EN 描述「NO ACCOUNT, NO CLOUD, NO NETWORK」段可追加一句:`On Android, Draftbook does not even request the internet permission.`
- 中文描述对应段可追加:`在安卓上，Draftbook 连联网权限都没有申请。`
- App 只支持竖屏手机布局;Play 的平板截图位留空,不写「平板」。

---|---|---|
| 1 | 大纲:章节卡片展开、手指正在拖一个场景 | Rearrange your whole book in a minute / 一分钟重排整本书 |
| 2 | 编辑器全屏写作,键盘上方工具条在位 | The toolbar is always there / 工具条随时都在 |
| 3 | 首页 hero:墨迹笔画 + 今日字数 + 连续天数 | A daily goal you can see / 看得见的每日目标 |
| 4 | 版本历史列表 + 一条版本的预览 | Undo a bad afternoon / 撤回写砸的那个下午 |
| 5 | 导出面板(Markdown / TXT / Word) | Export the whole manuscript / 整本导得出去 |
| 6 | 飞行模式图标 + 正常写作界面 | No account, no network / 没有账号,不联网 |

> 🔴 **截图是本项目当前最大的上架阻塞类型**(09-13 有 3 个 app 卡在 `0 of 10 Screenshots`)。本项 G6b 出包后**当轮就把 6 张拍完**,不留到提交前。

---

## § 附:M2 上线后再启用的段落(现在**不得**使用)

同步、账号、云端版本历史、资料板的文案在 M2/M3 真正发布后才可以填进商店。原文保留在下面,连同它对应的里程碑:

- **M2(同步 + 账号 + Android)**:
  - EN 段落:`YOUR DRAFT, NOT YOUR CLOUD ACCOUNT … Turn on sync and your projects move between your phone, your tablet and your other devices through our own service — no third-party storage subscription required.`
  - Pro 条目:`Sync across all your devices` / `30 days of cloud version history`
  - 副标题可改回 `Chapters, outline & sync`,关键词补 `sync`
  - ⚠️ 同时须补 App Store 数据收集声明(邮箱 + 用户内容),并加上 INTERNET 权限 —— 那一版的「不联网」文案必须整段删掉,不能留。
- **M3(资料板 + 编译输出)**:Pro 条目 `Research board for notes, images and character cards`。
