# CI-only values: builds always run on GitHub's 16GB Linux runners, never on
# the 8GB dev machine (see PIPELINE.md). Orbit pulls in geolocator,
# flutter_compass and sensors_plus, so R8 needs more metaspace than the shell
# default or the daemon dies with `OutOfMemoryError: Metaspace`
# (same bump as apps/fieldstamp and apps/goldenscout).
org.gradle.jvmargs=-Xmx4096m -Xms512m -XX:MaxMetaspaceSize=1024m -XX:ReservedCodeCacheSize=256m
org.gradle.parallel=false
org.gradle.workers.max=2
org.gradle.daemon=false
kotlin.daemon.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
