# toolapp — 工具型 App 热点情报 + 日报流水线

每天自动采集 GitHub / App Store / Google Play / Hacker News 的工具类热点，产出一份分析报告 + 一个 app 原型设计。最终目标：从报告中选题 → AI 开发无服务端工具 app → 自动上架（二期/三期，见下）。

## 目录结构

```
collectors/          # 采集脚本(Node 24+, ESM)
  run_all.mjs        # 入口:跑全部采集器,写 data/YYYY-MM-DD/*.json
  github_trending.mjs   # GitHub trending 页抓取(daily+weekly)
  appstore_rss.mjs      # Apple 官方 RSS(us/cn × top-free/top-paid,含工具类标记 isToolLike)
  google_play.mjs       # google-play-scraper(us × tools/productivity/photography TOP_FREE)
  hn_showhn.mjs         # HN Algolia API,近7天 Show HN ≥50分
data/YYYY-MM-DD/     # 当日原始数据(json,gitignore 不入库)
reports/YYYY-MM-DD.md  # 当日报告(最终交付物,推 GitHub)
shell/               # Flutter 壳工程模板(见 PIPELINE.md)
apps/<name>/         # 每个生成的 app 一个独立文件夹(推 GitHub)
scripts/new_app.mjs  # 克隆壳→新 app
```

远程仓库:`https://github.com/noobclaw/laura`(main)。报告与 apps/ 每次产出后都要 push。

## 每日定时任务的作业流程

1. `node collectors/run_all.mjs`(需网络,沙箱拦截时用 dangerouslyDisableSandbox)。看 stdout:允许个别源失败(记入 summary.json),全失败才算任务失败。
   - **⚠️(2026-09-01 事故后补)跑完必须确认两件事**:① 输出目录名 == **今天的本地日期**;② **前一日目录的 mtime 仍是前一日**(没被覆盖)。由来:采集器原先用 **UTC 日期**命名目录,09-01 在本地 07:34(= 23:34 UTC 08-31)运行时落进 `data/2026-08-31/` 并**覆盖了昨日快照**,导致当日换血度自检不可算。已改为本地日期,但**这类事故不体现在任何健康检查里** —— 当时采集器 4/4 全绿、`summary.json` 完全干净。
   - **⚠️(2026-09-01 同日第二次运行后补)当天已有报告时不要重写,改为「复核 + 追加」**。定时任务当日触发了两次,第二次跑完发现 `reports/2026-09-01.md` 已存在且完整 → 处置是**保留主体、追加一节复核**,并在报告里逐条并列「主体记 07:34 / 复核记 11:03」的差异,**不回改主体读数**(回改会掩盖掉两份读数之间的差本身就是结论)。**重采会覆盖 `data/<今天>/`,所以复核前先确认手上有没有更早那份快照、它躺在哪个目录**(当天两份快照可能分别躺在 `data/昨天/` 和 `data/今天/`,不要按目录名认日期,一律看 `fetchedAt`)。
   - **⚠️(2026-09-01)榜单位次读数按带分级,见纪律 AO(BACKLOG.md 顶部)**。同日双采样实测:付费榜 **#1-50 带 3.5 小时零掉榜、位次平均只动 0.8~3.2 位**,而 **#76-100 带 3.5 小时掉榜 21%/24%**。**每日「新进 Top100」几乎全部落在 #76-100 带 → 「今日新进」很大程度上是采样时刻的函数。** 新进条目照筛(筛形态与在位者结构),但**「今日进了 Top100」这句话不得作为证据**。⚖️ 同期 HN 字节级一致、GitHub 差 2 字节 —— **噪声只在 App Store 付费榜尾部**;评价数/评分/版本日期/定价**零变化,仍是最可信读数**。
2. 读当日 `data/YYYY-MM-DD/` 下各 json(大文件别整读,用 node -e 浓缩打印再分析)。
   - **(2026-09-05 用户拍板)调研重心放在 GitHub:找「优秀开源项目 → 能做成纯本地工具 app」。** 数据在 `github_search.json`(`collectors/github_search.mjs`,按 40 余个能力问题查 Search API:OCR/PDF/图像/音频/转换/加密/记账/天文/端侧模型…,每条带 stars/license/licenseClass/topics/pushedAt)+ `github.json`(trending)。评估一个 GitHub 项目是否值得移植,按顺序问:① **核心是不是纯本地计算**(算法/文件处理/端侧模型;需要服务端的直接跳过);② **许可证**:`permissive`(MIT/Apache/BSD/MPL)可直接内嵌 + 署名;`copyleft`(GPL/AGPL)只能借「思路」重实现或整 app 开源,报告里必须标明;`none` 视同不可用;③ **手机端有没有像样的对应物**:去 App Store/Play 数据里查同能力的 app 评分/价格/评价数(缺口判据同「选题硬性标准」);④ **移植量**:Dart/Kotlin/Swift/Rust/C++ 库 → 小;Python/JS 算法 → 需重写,按行数估;整套桌面 app → 只取核心能力;⑤ **维护度**:pushedAt 一年内、issues 不失控。GitHub 星数只证明「开发者喜欢」,**不证明消费者付费**,付费验证仍必须回到商店榜。
3. 与**前几天的报告**(reports/ 下最近 2-3 份)对比,识别「新上榜/持续升温/掉榜」。
4. 写 `reports/YYYY-MM-DD.md`,格式见下。
5. **推 GitHub**:本仓库远程是 `https://github.com/noobclaw/laura`(main 分支)。`git add reports/ apps/ && git commit && git push origin main`(commit message 全英文;push 前先 `git fetch` 确认 fast-forward;PowerShell 里 push 的红字 NativeCommandError 是假错误,以 `git log origin/main` 为准)。
6. 报告写完后,在回复里给用户一段 5 行以内的中文摘要(今日最值得做的 1 个选题 + 理由)。

## 报告格式(reports/YYYY-MM-DD.md)

```
# 工具 App 情报日报 — YYYY-MM-DD
## 一、今日信号(各源要点 + 与昨日对比)
## 二、趋势主题(跨源交叉出现的 2-4 个主题)
## 三、候选 App 清单(3-5 个,每个含:一句话定位/核心功能/目标用户/竞品与其弱点/变现/无服务端可行性/开发量估计)
## 四、今日原型(从候选中选 1 个展开:页面结构、核心交互流程、技术要点、商店定位[分类/关键词/定价])
## 五、观察池(还不够成熟但值得跟踪的信号)
## 六、GitHub 可移植项目(2026-09-05 起必写;每天 3-5 个:仓库/星数/许可证类别/核心能力一句话/手机端对应物与缺口/移植量估计/结论[进候选|观察|否决及原因])
```

## App 落地节奏(用户 2026-07-07 拍板)

- **每日选题进队列**:每天日报产出后,更新 `BACKLOG.md`(候选队列):合并当日新候选与既有队列,基于**全部历史数据**(目标过去 1~4 个月;不足用现有全部)按「值得做」**重新排序**。排序依据:①信号持续性(出现天数/多源交叉,权重最高) ②付费意愿验证(付费榜有同类) ③开发量(小优先) ④质量缺口(竞品评分低)。
- **每 3 天落地 1 个**(定时任务 `app-factory-3d`):**不自己选题,直接取 BACKLOG.md 排名最前的「待做」项**开发,开发全自动,完成后通知用户装机验收。**开发全程必须逐条执行 PIPELINE.md 的「⭐工厂标准作业流程 SOP」(G0-G8:防撞车/取题否决/立项评审/多语言+视觉设计/审计/CI冒烟/UI美观二次check/用语纪律/变现标准),缺一不可。** 其中 **UI 一定要美**——G3 按「视觉设计标准」做、G6b 出包后二次 check 截屏「好不好看」,丑就打回重做,和功能不成立同罪。
- **严禁重复**:BACKLOG 里已做项标 `✅已做`、否决项标 `❌否决`(附一句原因),永不删行;新候选与已做/否决项相同或高度相似的不入队。

### BACKLOG.md 格式(值得做的 App 汇总)

结构(每日维护,已有文件为准):
1. **总览表**:`| 排名 | App | 指数 | 状态 | 定价 | 价值预估(月) | 产品规划 |`
2. **逐项明细**:每个 app 一节,含 **指数拆解**(信号持续性40+付费验证25+开发可行性20+竞品缺口15=100,逐项给分并写依据)、**理由**(为什么值得做)、**价值**(定价×保守下载区间→月收入区间,注明非承诺)、**状态**。
3. **观察池**(未入队信号)与**已做/否决存档**(永不删,✅已上架/❌否决附原因)。

状态图例:🟢在做 ⚪排队 🧪待验收(代码完成、未经真机审计) 📦可发布(真机审计无 🔴) ✅已上架 ❌否决 ⛔搁置。**(2026-09-02 纪律 AQ① 起,`✅已做` 拆为 `🧪待验收` / `📦可发布` 两态 ——「CI 绿 + 代码写完」不得推定为可发布。)** 在做/已做的 app 必须在总览表挂 `apps/<name>/PLAN.md` 链接。
每日更新动作:新候选评分入队、既有项按最新信号调分重排、状态流转(排队→在做→已上架)。指数变动 ±5 以上要在当日日报里说一句原因。
- 每个 app 文件夹里必须有 `PLAN.md`(定位/功能清单/页面/定价)和 `store/`(商店文案)。
- 变现默认「免费+一次性内购买断」;走量品类可用「免费+去广告内购」;定价写进 PLAN.md。
- 上架节奏与开发节奏分离:开发 3 天一个,**上架仍每周 1-2 个**(商店反垃圾),做好的 app 排队等发布。

## 选题硬性标准

- **必须无服务端**:纯本地逻辑(计算/文件处理/相机/传感器/本地模型),不依赖自建后端。可用系统能力与设备端模型。
- **(2026-09-05)来自 GitHub 的选题额外两条**:① 许可证决定「怎么用」,不决定「做不做」:permissive(MIT/Apache/BSD/MPL)可直接依赖或内嵌 + 署名;**非 permissive(GPL/AGPL/无许可证)的项目一样进候选、一样排名——用户 09-05 原话「没有 MIT 的就你去看代码,自己改写一遍」**。做法固定为两步,写进 PLAN.md:先读其核心代码,把算法/数据结构/边界处理写成一份**自己的中文规格说明**(PLAN.md「参考实现分析」节:输入输出、步骤、常量、坑);再**只看规格、不看原码**用 Dart 从零实现,文件结构、命名、UI 全部是自己的,不复制任何一行、不移植其资源文件(模型权重/词库/图标另找 permissive 来源或自制)。PLAN.md 明记「参考 owner/repo(GPL),算法按上述规格重写,未复制代码与资源」。② BACKLOG 条目标 `[GH:owner/repo]`,让工厂知道有现成参考实现可读(读它的算法/边界处理,不抄它的 UI)。
- 工具型:解决一个具体小问题,单手可描述清楚。
- **(2026-08-30 放宽,用户拍板)「单手可描述的小工具」不再是一票否决项。** 触发:连续 17 日 ⚪排队 = 0 后的专项扩搜(74 组全新主词、跨 12 个零覆盖品类)显示,**唯一「付费意愿极强 + 质量缺口极大」的一块地(无障碍/特殊教育,$99.99~$299.99 × ★3.6~3.9 × 在积极维护)同时满足其余全部硬标准,唯独卡死在本条**。放宽口径四条:
  - **① 只放宽「开发量大」,不放宽「能力天花板」。** 沿用 08-13 判据:**做出来也不可能好**(受平台/物理层限制 —— 无 root 的照片恢复、无出厂标定的绝对量传感器测量[纪律 X])→ **仍直接否决**;**技术上做得到、只是工程量大**(多屏/编辑器/词库/无障碍适配)→ **不再因此否决**。**⚠️ 历史否决须按此重判**:08-29 把 `AstroShader`(天文图像配准/叠加)记为「能力天花板型」是**误判** —— 手机端叠加是纯算法且 `Star Stacker` 已做到,它属「开发量大」→ 该项须重新评估。
  - **② 其余三条硬标准一条不放宽**:无服务端、有买断付费面、系统自带/免费大厂替代不了(纪律 AG)。
  - **③ 大型项目按里程碑占用多轮工厂**:`PLAN.md` 须拆 M1/M2/M3,每轮 3 天工厂交付一个里程碑,**期间该项保持 🟢在做 并继续占队首,不算重复取题**(G0 防撞车按里程碑计,不按 app 计)。**没拆里程碑的大型项不得进工厂。**
  - **④ 涉及残障人士的品类须额外过一道伤害检查**:AAC / 盲文 / 医疗判读类,做错了有真实伤害 —— **不得以「先上线再迭代」的方式发布**,且不得触碰需器械认证或保险报销资质的形态。
  - **⚠️ 放宽当日的第一个受检对象 AAC(辅助沟通)仍被否决**:死因不是开发量,是龙头 `Proloquo2Go` **$249.99 / ★4.77 / 12,059 条 / 2009 年上架 / ver 2026-07-22 在维护** = 缺口 0,属「付费真、缺口假」型(免费侧 `Weave Chat AAC` ★4.67/505 两天前更新、`Visuals2Go` ★4.30/1,364;中价 `Speech Assistant AAC` $24.99/★4.56/103)。**本条放宽解除的是一条结构性约束,不是给某个具体候选开门。**
- 有付费/内购空间(参考 top-paid 榜验证付费意愿)。
- 避开垃圾赛道:清理加速类、壁纸类、山寨 VPN。
- 加分:多平台同时出现信号;现有头部竞品评分 < 4.0(质量缺口)。
- **(2026-07-24 收紧,闹钟教训)系统自带 app 能基本替代的直接否决**(闹钟/计时器/手电筒/计算器等),除非有付费榜实证的差异化切口。
- **(2026-07-24 收紧)自动流水线只取「缺口 ≥5/15 且付费验证 ≥15/25」的选题**;低于线的(如原干净闹钟 0/15+10/25)最多进观察池,不进 3 天工厂——做出来也是既难卖又拉低平均质量。BACKLOG 排序时同步执行。
- **必须写下「用户为什么选我而不是竞品」一句话**(差异化楔子)进 PLAN.md;写不出来 = 不该做。品类有人付费 ≠ 我的 app 有人付费,还差「被发现(ASO)+被选中(差异化)」两关。

## 采集器备注(踩过的坑)

- HN Algolia 的 numericFilters 不再支持 `points`,只能按 `created_at_i` 过滤、分数本地过滤。
- App Store CN 区 RSS 返回中文分类名(工具/效率/摄影与录像),TOOL_GENRES 已含。
- appstore.json 用 PowerShell 5.1 的 ConvertFrom-Json 会假报错,验证 JSON 一律用 `node -e "require(...)"`。
- GitHub trending 是抓 HTML,解析 0 条 = 页面结构变了,要修 `parseTrendingHtml`。
- google-play-scraper 是非官方库,偶发被限流;单类目失败不致命(errors 字段记录)。

## 后续阶段(未做)

- 二期:Flutter 壳工程 + 「选题 → AI 生成完整 app + 商店素材」流水线 + CI 构建。
- 三期:fastlane 自动提交上架(先 Google Play 后 App Store)。用户已有开发者账号。
- 节奏约束:商店反垃圾政策(Apple 4.3 / Google 重复内容),上架节奏每周 1-2 个,不可每日上架。
